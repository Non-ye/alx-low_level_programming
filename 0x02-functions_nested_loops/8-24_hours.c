#include "main.h"
/**
 * jack_bauer - Entry
 * Return: No return
 */
void jack_bauer(void)
{
	int e, f, g, h;

	for (e = '0'; e <= '2'; e++)
	{
		for (f = '0'; f <= '9'; f++)
		{
			for (g = '0'; g <= '5'; g++)
			{
				for (h = '0'; h <= '9'; h++)
				{
					if (e >= '2' && >= '4')
						break;
					_putchar(a);
					_putchar(b);
					_putchar(':');
					_putchar(c);
					_putchar(d);
					_putchar('\n');
				}
			}
		}
	}
}
