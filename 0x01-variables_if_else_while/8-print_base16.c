#include <stdio.h>
/**
 * main - Entry
 * Return: to 0
 */

int main(void)
{
	char g;

	char h;

	g = 'a';
	while
		(h < 10)
		{
			putchar(h + '0');
			h++;
		}
	while
		(g <= 'f')
		{
			putchar(g);
			g++;
		}
	putchar('\n');
	return (0);
}
