#include <stdio.h>
/**
 * main - Entry
 * Return: to 0
 */

int main(void)
{
	int a;

	a = 0;
	while
		(a < 100)
		{
			putchar(a + '0');
			putchar(',');
			putchar(' ');
			a++;
		}
	putchar('\n');
	return (0);
}
