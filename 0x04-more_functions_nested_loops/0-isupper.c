#include "main.h"

/**
 * _isupper - check the code
 * @c: The character to print
 * Return: to 1 if c is uppercase, 0 if otherwise
 */

int _isupper(int c)
{
	if (c == 'A')
	{
		return (1);
	}
	else
	{
		return (0);
	}
	_putchar('\n');
}
